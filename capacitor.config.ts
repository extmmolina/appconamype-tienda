import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appconamype tienda',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
